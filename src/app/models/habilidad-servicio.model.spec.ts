import { HabilidadServicio } from './habilidad-servicio.model';

describe('HabilidadServicio', () => {
  it('should create an instance', () => {
    expect(new HabilidadServicio()).toBeTruthy();
  });
});
